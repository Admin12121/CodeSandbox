"""Seed modules for one-time local/bootstrap data.

The root ``seed.py`` script remains the executable entrypoint. Keep feature
seed logic in this package so admin/RBAC/bootstrap data does not grow together
with sandbox template examples.
"""

